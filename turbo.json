{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["VITE_BASE_PATH"],
      "outputs": ["dist/**", "out/**", "template/**"]
    },
    "test": {
      "dependsOn": ["^build", "build"]
    },
    "lint": {
      "dependsOn": ["^build", "build"]
    },
    "format": {},
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
